<% if (site.tags.length){ %>
  <div class="widget">
    <h3 class="title"><%= __('tagcloud') %></h3>
    <div class="content tag-cloud">
        <div <% if(theme.tagcloud.tag3d){ %> id="tag-cloud-3d" <% }%> >
            <%-tagcloud() %>
        </div>
    </div>
  </div>
<% } %>

